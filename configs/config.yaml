bot:
  token: "YOUR_BOT_TOKEN"
  timeout: 60

redis:
  host: "localhost"
  port: 6379
  password: ""
  db: 0

auth:
  code_length: 6
  code_ttl: 300 # 5 minutes
  max_attempts: 3
  sms_gateway_url: "https://sms-gateway.example.com/send"

server:
  port: ":8080"
  debug: true